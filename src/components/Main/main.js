import React from "react";
import { View, Text } from "react-native";
import Form from "../Form/form";

const Main = () => {
  return (
    <View>
      <Form />
    </View>
  );
};

export default Main;
